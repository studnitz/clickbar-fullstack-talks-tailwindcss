import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, nightOwl } from "@code-surfer/themes";

export const theme = nightOwl;

# Tailwind CSS

## Schnell moderne Webseiten bauen 💨

---

# Ich bin Alex

- 🧑‍💻 Arbeite bei clickbar.
- 👨‍🎓 Studiere Informatik
- 🕺 Tanze Swing in meiner Freizeit
- 🐥 [bin auf Twitter](https://twitter.com/jvstudnitz)

---

# Was ist Tailwind CSS?

---

# Tailwind CSS ist Utility-first

---

# Tailwind CSS ist Utility-first

Sehr viele Klassen, die direkt auf eine CSS-Property und einen Wert mappen.

<CodeSurferColumns themes={[nightOwl, github]}>

<Step subtitle="Margin">

```html 1[21:23]
<button class="flex m-8 rounded-full hover:bg-black">
```

```css
.m-8 {
    margin: 2rem;
}
```

</Step>

<Step subtitle="flex">

```html 1[16:20]
<button class="flex m-8 rounded-full hover:bg-black">
```

```css
.m-8 {
    margin: 2rem;
}

.flex {
    display: flex;
}
```

</Step>

<Step subtitle="Border-Radius">

```html 1[25:37]
<button class="flex m-8 rounded-full hover:bg-black">
```

```css
.m-8 {
    margin: 2rem;
}

.flex {
    display: flex;
}

.rounded-full {
    border-radius: 9999px;
}
```

</Step>

<Step subtitle="Pseudo-Styles">

```html 1[38:52]
<button class="flex m-8 rounded-full hover:bg-black">
```

```css
.m-8 {
    margin: 2rem;
}

.flex {
    display: flex;
}

.rounded-full {
    border-radius: 9999px;
}

.bg-black:hover {
    background-color: #000000;
}
```

</Step>

</CodeSurferColumns>

---

# Tailwind CSS ist eine API für ein Designsystem

Viele Möglichkeiten, aber nicht unendlich viele.

---

<CodeSurfer>

```js title="Tailwind CSS ist einfach zu erweitern"
// tailwind.config.js
module.exports = {
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}

```

```js title="Tailwind CSS ist einfach zu erweitern" subtitle="Fonts austauschen"
const defaultTheme = require('@tailwindcss/defaultTheme')

// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
```

```js title="Tailwind CSS ist einfach zu erweitern" subtitle="Farben austauschen"
const defaultTheme = require('@tailwindcss/defaultTheme')
const colors = require('@tailwindcss/colors')

// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      colors: {
        green: colors.emerald
      },
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
}

```

```js title="Tailwind CSS ist einfach zu erweitern" subtitle="Plugins hinzufügen"
const defaultTheme = require('@tailwindcss/defaultTheme')
const colors = require('@tailwindcss/colors')

// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      colors: {
        green: colors.emerald
      },
    },
  },
  variants: {
    extend: {},
  },
  plugins: [require('@tailwindcss/typography')],
}

```

</CodeSurfer>

---

# Was ist Tailwind CSS nicht?

---

# Tailwind CSS ist keine Component-Library

- Keine `.card` oder `.button`-Klassen
- [Tailwind UI](https://tailwindui.com) ist eine coole von den Entwicklern von Tailwind CSS

---

# Tailwind CSS ist keine Inline-Styles

---

# Tailwind CSS ist nicht die Lösung für jeden

🤷‍♂️

---

# Lasst uns eine Card-Komponente bauen

<div class="chat-notification">
  <div class="chat-notification-logo-wrapper">
    <img class="chat-notification-logo" src="/img/logo.svg" alt="ChitChat Logo" />
  </div>
  <div class="chat-notification-content">
    <h4 class="chat-notification-title">ChitChat</h4>
    <p class="chat-notification-message">You have a new message!</p>
  </div>
</div>

---

<CodeSurfer>

```html
<div class="chat-notification">
  <div class="chat-notification-logo-wrapper">
    <img class="chat-notification-logo" src="/img/logo.svg" alt="ChitChat Logo">
  </div>
  <div class="chat-notification-content">
    <h4 class="chat-notification-title">ChitChat</h4>
    <p class="chat-notification-message">You have a new message!</p>
  </div>
</div>

<style>
  .chat-notification {
    display: flex;
    max-width: 24rem;
    margin: 0 auto;
    padding: 1.5rem;
    border-radius: 0.5rem;
    background-color: #fff;
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
  .chat-notification-logo-wrapper {
    flex-shrink: 0;
  }
  .chat-notification-logo {
    height: 3rem;
    width: 3rem;
  }
  .chat-notification-content {
    margin-left: 1.5rem;
    padding-top: 0.25rem;
  }
  .chat-notification-title {
    color: #1a202c;
    font-size: 1.25rem;
    line-height: 1.25;
  }
  .chat-notification-message {
    color: #718096;
    font-size: 1rem;
    line-height: 1.5;
  }
</style>
```

</CodeSurfer>

---

# Danke!

## 🙏

## [studnitz.dev](https://studnitz.dev)

## [@jvstudnitz](https://twitter.com/jvstudnitz)

---
